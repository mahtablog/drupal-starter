{% import '@server_theme/templates/server-theme-inner-element-layout-base.html.twig' as base %}
{% macro getClass(padding) %}
  {% set classes = [
    'grid place-items-center',
    padding != 0 ? padding : 'p-4',
  ] | join(' ') | trim %}
  {{ classes }}
{% endmacro %}

<div class="{{ base.getBaseClasses()}} {{_self.getClass(padding) }} ">

  {{ items }}

</div>
